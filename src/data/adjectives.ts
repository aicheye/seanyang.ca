import data from '@public/data/adjectives.json'

/** Where I am right now, shown next to my name in the header. */
export const location: string = data.location

export default data.adjectives as string[]
