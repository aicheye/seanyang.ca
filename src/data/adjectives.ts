import data from '@public/data/adjectives.json'

/** Where I am right now, baked into the page description. The header reads
    the same file live through HeaderBio so the mirrors pick up moves. */
export const location: string = data.location
